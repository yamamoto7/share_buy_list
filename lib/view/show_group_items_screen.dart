import 'package:flutter/material.dart';

class ShowGroupItemsScreen extends StatefulWidget {
  const ShowGroupItemsScreen({Key? key}) : super(key: key);

  @override
  _ShowGroupItemsScreenState createState() => _ShowGroupItemsScreenState();
}

class _ShowGroupItemsScreenState extends State<ShowGroupItemsScreen> {
  @override
  void initState() {
    super.initState();
  }

  @override
  void dispose() {
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: const Text("place holder"),
    );
  }
}
